# Mobile client boundary

The future mobile client consumes the same shared protocol contracts as web and
desktop. It must implement suspension, network-handoff, secure-keystore, and
push boundaries before it can participate in a huddle.
